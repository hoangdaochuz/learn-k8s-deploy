apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-golang-deployment
  namespace: demo-app
spec:
  replicas: 2 #base-replicas
  template:
    spec:
      containers:
        - name: api-golang
          resources:
            requests:
              memory: "100Mi"
              cpu: "100m"
            limits:
              memory: "200Mi"
              cpu: "250m"
