apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: traefik-middleware-stripprefix
spec:
  stripPrefix:
    forceSlash: false
    prefixes:
      - /api/golang
      - /api/node
